########
# KEYS #
########

# keys = [
# Key([mod], "h", lazy.layout.left(), desc="Move focus to left"),
# Key([mod, "shift"], "h", lazy.layout.shuffle_left(), desc="Move window to the left"),
# Key([mod, "control"], "h", lazy.layout.grow_left(), desc="Grow window to the left"),
# Key([mod], "n", lazy.layout.normalize(), desc="Reset all window sizes"),
# Toggle between split and unsplit sides of stack.
# Split = all windows displayed
# Unsplit = 1 window displayed, like Max layout, but still with
# multiple stack panes
# Key([mod, "shift"], "Return", lazy.layout.toggle_split(), desc="Toggle (un)split",),
# Key([mod], "Tab", lazy.next_layout(), desc="Toggle layouts"),
# Key([mod], "t", lazy.window.toggle_floating(), desc="Float",),

# Key([mod, "control"], "q", lazy.shutdown(), desc="Shutdown"),
# Key([mod], "r", lazy.spawncmd(), desc="Spawn a command using a prompt widget"),
# ]

#########
# MOUSE #
#########

# Drag floating layouts.
# mouse = [
#     Drag(
#         [mod],
#         "Button1",
#         lazy.window.set_position_floating(),
#         start=lazy.window.get_position(),
#     ),
#     Drag(
#         [mod], "Button3", lazy.window.set_size_floating(), start=lazy.window.get_size()
#     ),
#     Click([mod], "Button2", lazy.window.bring_to_front()),
# ]
